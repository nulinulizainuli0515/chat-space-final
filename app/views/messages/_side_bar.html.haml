-# <サイドバー>
.side
  .side-header
    .side-box
      %h3.side-box__name
        = current_user.name
      %ul.side-box__icon
        %li.list
        = link_to new_group_path do
          = icon('fas','pen-square', class: "side-btn__icon")
        %li.list
        = link_to edit_user_path(current_user) do
          = icon('fas','cog', class: "side-btn__icon")
  .side-groups
    - current_user.groups.each do |group|
      .group
        = link_to '#' do
          .group__name
            = group.name
          .group__message
            メッセージはまだありません。

